# PayShield deployer config (repo root — used by Hardhat)
# Copy to .env.local and set your key. NEVER commit real keys.

# Required for Arbitrum Sepolia deploy
DEPLOYER_PRIVATE_KEY=

# Optional overrides
ARBITRUM_SEPOLIA_RPC_URL=https://sepolia-rollup.arbitrum.io/rpc
